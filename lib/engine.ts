import type { ReadyEvent,Recommendation,SleepContext,TodoistTask,WeatherContext } from "./types";
export function buildRecommendations(events:ReadyEvent[],tasks:TodoistTask[],weather:WeatherContext,sleep:SleepContext){const recs:Recommendation[]=[];const add=(r:Omit<Recommendation,"completed"|"dismissed">)=>recs.push({...r,completed:false,dismissed:false});const exerciseEvent=events.find(event=>event.type==="exercise");if(weather.uvIndex>=6)add({id:"sunscreen",category:"do",title:"Apply sunscreen",description:"Before you head out",reason:`UV index ${weather.uvIndex} is expected today.`,sources:["Weather"],priority:"essential",confidence:.92});if(events.length>=3)add({id:"lunch",category:"eat",title:"Pack lunch",description:"A simple lunch for your scheduled day",reason:`You have ${events.length} scheduled events today.`,sources:["Calendar"],priority:"helpful",confidence:.86});tasks.filter(task=>task.dueToday&&task.physicalItem).forEach(task=>add({id:task.id,category:"bring",title:`Bring ${task.physicalItem}`,description:task.title,reason:"This task requires a physical object to complete.",sources:["Todoist"],priority:"essential",confidence:.96}));if(exerciseEvent)add({id:"gym",category:"bring",title:"Pack gym clothes",description:"Workout top and leggings",reason:`${exerciseEvent.title} is classified as exercise, so workout clothing may be useful.`,sources:["Calendar"],priority:"essential",confidence:exerciseEvent.typeConfidence});if(weather.precipitationStart)add({id:"umbrella",category:"bring",title:"Bring umbrella",description:`Rain expected after ${weather.precipitationStart}`,reason:"Rain is expected later today, so an umbrella may be useful.",sources:["Weather"],priority:"essential",confidence:.94});if(sleep.durationMinutes>=sleep.baselineMinutes-60)add({id:"phone",category:"do",title:"Charge phone",description:"Top up before leaving",reason:"Your current information load allows this optional routine reminder.",sources:["Sleep","Routine"],priority:"optional",confidence:.7});return recs;}
